# Simple Chat application with WebSockets and EventMachine

To set up, run `bundle install`

To start the Web socket server, run `ruby server.rb`

To Start the Web server, run `run rackup`

#### Have Fun!!
